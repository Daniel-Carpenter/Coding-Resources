source('~/.active-rstudio-document', echo=TRUE)
source('~/.active-rstudio-document', echo=TRUE)
install.packages("Rcpp", type="source")
install.packages("https://ampl.com/dl/API/rAMPL.tar.gz", repos=NULL)
library(rAMPL)
# Create an AMPL instance
# env  <- new(Environment, "full path to the AMPL installation directory")
ampl <- new(AMPL, env)
# Create an AMPL instance
# env  <- new(Environment, "full path to the AMPL installation directory")
ampl <- new(AMPL)
setwd('C:/Users/daniel.carpenter/Downloads/Example Functions')
getwd()
library(tidyverse)
sqRootFunction <- function(x) {
# Calculate the output
output = x^2
return(output)
}
sqRootFunction(2)
sqRootFunction(8)
xValues <- 1:10 #c(1, 2, 3, ..., 10)
length(xValues)
# Iterate over the function from 1 to 10
for (x in 1:length(xValues)) {
sqRootFunction(x)
}
# Iterate over the function from 1 to 10
for (x in 1:length(xValues)) {
sqRootFunction(x)
}
# Iterate over the function from 1 to 10
for (x in 1:length(xValues)) {
sqRootFunction(x)
}
# Iterate over the function from 1 to 10
for (x in 1:length(xValues)) {
print(sqRootFunction(x))
}
# Create a data frame
df <- c()
df
# Create a data frame
df <- c()
# Iterate over the function from 1 to 10
for (x in 1:length(xValues)) {
df <- rbind(df, sqRootFunction(x))
}
# Print it, show that it looks the same as above
df
forecast <- function(SOURCE_PROP) {
# Function here
ifelse(SOURCE_PROP == 'RS', 1, 0)
}
forecast('RS')
forecast('WS')
df <- c()
# Loop over list of businesses
for (SOURCE_PROP in 1:length(sourProps)) {
df <- rbind(df, forecast(SOURCE_PROP))
}
df
sourProps <- c('RS', 'SC', 'WS')
sourceProps <- c('RS', 'SC', 'WS')
forecast <- function(SOURCE_PROP) {
# Function here
ifelse(SOURCE_PROP == 'RS', 1, 0)
}
forecast('WS')
df <- c()
# Loop over list of businesses
for (SOURCE_PROP in 1:length(sourceProps)) {
df <- rbind(df, forecast(SOURCE_PROP))
}
df
# Loop over list of businesses
for (SOURCE_PROP in 1:length(sourceProps)) {
df <- rbind(df, forecast(sourceProps[SOURCE_PROP]))
}
df
df <- c()
sourceProps <- c('RS', 'SC', 'WS')
# Loop over list of businesses
for (SOURCE_PROP in 1:length(sourceProps)) {
df <- rbind(df, forecast(sourceProps[SOURCE_PROP]))
}
df
# Create the df
df <- data.frame()
sourceProps <- c('RS', 'SC', 'WS')
# Loop over list of businesses
for (SOURCE_PROP in 1:length(sourceProps)) {
df <- rbind(df, forecast(sourceProps[SOURCE_PROP]))
}
df
source("theFunction.R")
source("theFunction.R")
# Example of run the fun
forecast('WS')
# Create the df
df <- c()
sourceProps <- c('RS', 'SC', 'WS')
# Loop over list of businesses
for (SOURCE_PROP in 1:length(sourceProps)) {
df <- rbind(df, forecast(sourceProps[SOURCE_PROP]))
}
df
